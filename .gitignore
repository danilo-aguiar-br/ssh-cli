# Artefatos de build
target/
Cargo.lock.bak
*.rs.bk
# Cópia local acidental do binário release (polui git describe dirty)
/rust_out

# Logs
logs/
*.log

# Local MCP / tooling sidecars (full dir — not only *.log)
.setting.cyber/
# Staging de edição do atomwrite — jamais empacotado (cargo package --list o pegava)
.atomwrite/

# Secrets
*.pem
*.key
*.p12
*.pfx
.env
.env.*
.e2e.local
e2e-artifacts/
known_hosts

# Privado de desenvolvimento
.serena/
.claude/
/CLAUDE.md
docs_prd/
docs_rules/
projeto_ssh-cli.md
# Local audit inventory — never publish (GitHub / crates)
/gaps.md
.cursor/

# GraphRAG / filas de enriquecimento (artefatos locais — qualquer pasta)
**/graphrag.sqlite
**/graphrag.sqlite-shm
**/graphrag.sqlite-wal
**/graphrag.sqlite-journal
**/.enrich-queue.sqlite
**/.enrich-queue.sqlite-shm
**/.enrich-queue.sqlite-wal
**/.enrich-queue.sqlite-journal
*.7z

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Cobertura LLVM
*.profraw
*.profdata
coverage/
lcov.info

# Ralph loop
.claude/ralph-loop.local.md
ralph-loop.local.md

# Privado de desenvolvimento
/AGENTS.md
# deny.toml versionado com o projeto (supply-chain 0.3.8)
Cross.toml

*.7.z

# BEGIN GH_SKILL_HYGIENE
# Managed by ~/.grok/skills/gh/scripts/pre-commit-hygiene.sh — do not hand-edit inside markers
# Ensures agent residual, secrets, DBs, media dumps and temp dirs never enter git
# Empty/whitespace-only patterns are rejected (never added). No *.lock patterns.
# --- extensions ---
*.7z
*.log
*.env
*.png
*.jpg
# --- name contains OR extension: .bak ---
*.bak
*.bak.*
*.*.bak
*.*.bak.*
# --- name contains OR extension: .tmp ---
*.tmp
*.tmp.*
*.*.tmp
*.*.tmp.*
# --- name contains OR extension: .journal ---
*.journal
*.journal.*
*.*.journal
*.*.journal.*
# --- name contains OR extension: .atomwrite_tmp ---
*.atomwrite_tmp
*.atomwrite_tmp.*
*atomwrite_tmp*
# --- name contains: _tmp ---
*_tmp*
# --- name contains: .atomwrite- ---
*.atomwrite-*
*atomwrite-*
# --- name contains: .atom_ ---
*.atom_*
*atom_*
# --- name contains: .atom- ---
*.atom-*
*atom-*
# --- name contains: ._agent_tmp ---
*._agent_tmp*
._agent_tmp*
# --- name contains: agent_tmp ---
*agent_tmp*
# --- name contains: .tmp_ ---
*.tmp_*
*.*.tmp_*
# --- named files (any depth) ---
gaps.md
**/gaps.md
graphrag.sqlite
**/graphrag.sqlite
graphrag.sqlite.enrich-queue.sqlite
**/graphrag.sqlite.enrich-queue.sqlite
graphrag.sqlite-wal
**/graphrag.sqlite-wal
.enrich-queue.sqlite
**/.enrich-queue.sqlite
.enrich-queue.sqlite-shm
**/.enrich-queue.sqlite-shm
.ingest-queue.sqlite-shm
**/.ingest-queue.sqlite-shm
.ingest-queue.sqlite-wal
**/.ingest-queue.sqlite-wal
CLAUDE.md
/CLAUDE.md
.commit_hash
**/.commit_hash
# --- directories (prefix + contents) ---
base_*/
docs_rules*/
.atomwrite_*/
.atomwrite-*/
.atomwrite*/
.tmp*/
.atom_*/
.atom-*/
.agent_*/
_agent_*/
.staging*/
.tmp_*/
# --- exact directories + contents ---
.setting.cyber/
.setting.cyber/**
docs_rules_ingest/
docs_rules_ingest/**
.claude/
.claude/**
.skill-build/
.skill-build/**
.enrich-overnight/
.enrich-overnight/**
.archive/
.archive/**
uploads/
uploads/**
docs_bugs/
docs_bugs/**
# END GH_SKILL_HYGIENE
