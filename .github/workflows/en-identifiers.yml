name: en-identifiers
on:
  push:
    branches: [main, master]
  pull_request:
jobs:
  en-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: English identifier gate
        run: bash scripts/check_en_identifiers.sh
